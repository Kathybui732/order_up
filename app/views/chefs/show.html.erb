<%= @chef.name %>'s Show Page

<%= link_to "Chef's Ingredients", "/chefs/#{@chef.id}/ingredients" %>

<%# Most Used Ingredients:
<% @chef.most_used_ingredients.each do |ingredient| %>
<%# <%= ingredient.name %> %>
<%# <% end %> %> %>
